- name: snitch deployment
  hosts: snitch
  become: yes
  become_method: doas
  roles:
    - basic
    - users
    - sshd
  vars:
    users:
      - rilla
      - ansible
    use_lbu: true
  post_tasks:
    - name: lbu commit
      lbu:
        commit: true

- name: caladan deployment
  hosts: caladan
  become: yes
  become_method: doas
  roles:
    - basic
  vars:
    use_lbu: false
